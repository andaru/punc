base_path: /tmp/punc-workdir/
collections:
  default:
    recipes:
      - vendor: cisco
        regexp: ".*"
        ruleset: cisco
        path: cisco/
      - vendor: juniper
        regexp: ".*"
        ruleset: juniper
        path: juniper/
      - vendor: timetra
        regexp: ".*"
        ruleset: timetra
        path: timetra/
      - vendor: telco
        regexp: ".*"
        ruleset: telco
        path: telco/
